#include <iostream>
#include <vector>
using namespace std;
int main()
{
    int t;    // t is the number of test cases
    cin >> t; // read the number of test cases
    for (int i = 0; i < t; i++)
    {
        int n; // n is the number of friends alice has
        cin >> n;
        vector<int> location(n); // location of each friend
        for (int j = 0; j < n; j++)
        {
            cin >> location[j];
        }
        for(int i=0;i<n;i++){
            
        }
    }
}
